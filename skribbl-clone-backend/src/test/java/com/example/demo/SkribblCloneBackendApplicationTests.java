package com.example.demo;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

import com.skribbl.backend.SkribblCloneBackendApplication;

@SpringBootTest(classes = SkribblCloneBackendApplication.class)
class SkribblCloneBackendApplicationTests {

    @Test
    void contextLoads() {
    }
}
